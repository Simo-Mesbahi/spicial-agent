# Pre-LLM validation — 17 September 2026

Scope: current main after the user confirmed real MFA enrollment works. This is a regression pass, not proof of zero bugs or a production certification.

Corrections:
- Dashboard requests use a revision guard: older refresh/search responses cannot replace newer results or reset their busy/error state. Switching organizations clears old visible data; authorization remains server-side.
- A failed logout no longer silently claims success. The administrator gets a retry message and local identity is cleared only after the server confirms logout.
- MFA verification uses the same synchronous interaction lock as enrollment/resume, accepts exactly six digits and allows 60 seconds for the sequential bounded Auth calls.
- Recovery button has explicit contrast, 44px target and visible keyboard focus. MFA labels/instructions are more readable.
- Landing page email action explicitly says it opens email, avoiding confusion with the chatbot.

Validation:
- 156 application tests passed (API, simulated MFA challenge/verification, RAG/model policy, runtime settings, contact, client access and Cloudflare contract tests).
- TypeScript and application lint passed. Production build and 10 render/component tests passed.
- Browser: landing scenario transitions from waiting for part to repairing; contact route loads with percent-encoded subject/body; recipient copying confirmed after hydration; /file fails closed when this environment lacks Supabase configuration; demo opens and answers the return-policy question using its documentary response.
- First clipboard attempt before the hydrated contact view was ready did not confirm a copy; the fully loaded view succeeded. No email was sent.

Limits: real LLM provider calls, real Supabase dossier access and authenticated dashboard race/logout scenarios were not exercised in the browser because no real credentials are available here. MFA real-device success is reported by the user. No production deployment performed. Existing automated tests pass; the new frontend concurrency guards were reviewed but do not yet have dedicated interactive regression coverage.
