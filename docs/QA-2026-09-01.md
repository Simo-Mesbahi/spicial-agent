# Recette SAV SC Assistant AI du 1er septembre 2026

Périmètre : nouvelle identité, parcours Contact, durcissement des dépendances et non-régression de la démonstration SAV/service client. Les dossiers et les données restent fictifs.

## Résultats automatisés

| Contrôle                    | Résultat observé                         |
| --------------------------- | ---------------------------------------- |
| `npm run typecheck`         | Réussi                                   |
| `npm run lint:app`          | Réussi                                   |
| `npm test`                  | 87 réussis, aucun échec                  |
| `npm run test:starter`      | 5 réussis, aucun échec                   |
| `npm run build`             | Réussi, sorties client et serveur créées |
| `npm audit --omit=dev`      | Aucune vulnérabilité connue signalée     |
| Contrôle de diff et secrets | Réussi ; aucun secret d’API ajouté à Git |

La suite applicative comprend quatre nouveaux tests de contact : destination fixe, conservation de l’Unicode, adresse de réponse, neutralisation des retours de ligne et rejet des entrées invalides. Les 83 tests antérieurs continuent de couvrir l’API, les permissions, l’isolation, les devis, les transitions, les modèles, les quotas, l’historique et le rendu métier.

Les dépendances transitoires `fast-uri`, `nanoid` et `postcss` ont reçu leurs correctifs compatibles. Next.js et sa configuration ESLint ont été portés de 16.2.6 à 16.3.4 ; la compilation et l’intégralité des tests ont été rejouées après cette mise à jour. L’audit npm de production ne signale plus de vulnérabilité connue.

## Parcours navigateur contrôlé

- Titre et identité visibles : **SAV SC Assistant AI** sur l’accueil et dans l’espace de démonstration.
- Trois accès « Nous contacter » présents sur l’accueil ; ouverture correcte de la page dédiée.
- Destinataire affiché : `mohammed.elmesbahi31@gmail.com`.
- Champs email, objet et message associés à des libellés accessibles.
- Adresse email invalide refusée avec le message « Saisissez une adresse email valide. ».
- Thèmes clair et sombre inspectés ; formulaire, état d’erreur et boutons lisibles.
- Aucun débordement horizontal dans le viewport de bureau contrôlé.
- Aucun avertissement ni erreur provenant de l’application dans la console après le contrôle final.

Aucun email réel n’a été envoyé pendant la recette. Le lien `mailto:` est vérifié par les tests : le visiteur conserve l’action finale dans son application mail.

## Limites de cette recette

- Le comportement d’ouverture dépend de la présence d’une application mail configurée sur l’appareil. L’adresse peut également être copiée depuis la page.
- Safari/iPhone physique, lecteur d’écran, charge, reprise après arrêt brutal et audit de sécurité indépendant restent des validations séparées.
- La synchronisation GitHub, l’exécution distante de la CI et la publication du site sont des étapes distinctes du résultat local.
- Cette recette n’évalue pas Gemini réel et ne valide aucune utilisation avec des données clients réelles.
